# Как ставить задачи ИИ для автоматической работы с документацией

## ✅ Правила уже настроены!

ИИ автоматически читает правила из `.cursor/rules/` и следует им при каждой задаче. Вам **не нужно** каждый раз напоминать об обновлении документации.

## 📝 Как ставить задачи

### Вариант 1: Обычная задача (рекомендуется)

Просто опишите, что нужно сделать. ИИ автоматически:
- ✅ Обновит `CHANGELOG.md` (секция `[Unreleased]`)
- ✅ Добавит запись в `AI_WORKLOG.md`
- ✅ Обновит документацию в `docs/` (если нужно)
- ✅ Создаст коммит с правильным сообщением

**Примеры задач:**
```
Добавь экран настроек с возможностью изменения темы
```
```
Исправь баг с кодировкой в CHANGELOG
```
```
Добавь поддержку темной темы
```

ИИ сам определит тип изменений и обновит все артефакты.

### Вариант 2: Явное указание (если нужно)

Если хотите явно указать, что нужно обновить документацию:

```
Добавь функцию X и обнови документацию
```

Или используйте команды Cursor:
- `update-artifacts` - обновить все артефакты (CHANGELOG, AI_WORKLOG, docs)
- `update-changelog` - обновить только CHANGELOG
- `log-work` - добавить запись в AI_WORKLOG
- `commit-ready` - подготовить коммит (покажет сообщение, но не сделает коммит)

### Вариант 3: Без коммита (если нужно)

Если не хотите, чтобы ИИ делал коммит автоматически:

```
Добавь функцию X, но не коммить изменения
```

Или:
```
Сделай изменения, но оставь коммит на потом
```

## 🔄 Что происходит автоматически

При каждой задаче ИИ:

1. **Анализирует изменения:**
   - Определяет тип (feat/fix/docs/chore/refactor/test)
   - Поняет, какие файлы затронуты

2. **Обновляет артефакты:**
   - `CHANGELOG.md` → добавляет в секцию `[Unreleased]`
   - `AI_WORKLOG.md` → добавляет запись с датой, задачей, изменениями
   - `docs/*` → обновляет, если меняется поведение/инструкции

3. **Создает коммит:**
   - Формат: `<type>(client): <описание>`
   - Пример: `feat(client): добавлен экран настроек`

## 📋 Примеры правильных задач

### ✅ Хорошие задачи (ИИ все сделает сам):

```
Добавь валидацию email при регистрации
```
→ ИИ: добавит код, обновит CHANGELOG, AI_WORKLOG, сделает коммит `feat(client): добавлена валидация email`

```
Исправь баг с отображением уведомлений на Android 12
```
→ ИИ: исправит код, обновит CHANGELOG, AI_WORKLOG, сделает коммит `fix(client): исправлено отображение уведомлений на Android 12`

```
Добавь документацию по использованию WebSocket API
```
→ ИИ: создаст/обновит docs, обновит CHANGELOG, AI_WORKLOG, сделает коммит `docs(client): добавлена документация WebSocket API`

### ❌ Не нужно писать так:

```
Добавь функцию X, обнови CHANGELOG, добавь в AI_WORKLOG и сделай коммит
```
→ Это избыточно, ИИ и так все сделает автоматически

## 🎯 Специальные случаи

### Если нужно только обновить документацию:

```
Обнови документацию согласно последним изменениям
```

### Если нужно сделать несколько изменений в одном коммите:

```
Добавь функцию X и функцию Y
```
→ ИИ объединит в один коммит с описанием обеих функций

### Если нужно сделать несколько коммитов:

```
Сначала добавь функцию X, потом функцию Y. Каждую в отдельном коммите.
```

## 🔍 Проверка работы правил

Правила находятся в:
- `.cursor/rules/00-scope.md` - область работы (только клиент)
- `.cursor/rules/10-artifacts.md` - обязательные артефакты
- `.cursor/rules/20-auto-commit.md` - автоматический коммит

ИИ читает эти файлы автоматически при каждой задаче.

## 💡 Совет

**Просто ставьте задачи естественным языком** - ИИ сам поймет, что нужно делать, и автоматически обновит всю документацию и создаст коммит.

Если что-то пошло не так, можно явно попросить:
```
Обнови CHANGELOG и AI_WORKLOG для последних изменений
```

