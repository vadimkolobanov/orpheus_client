# Быстрый старт: Запуск тестов и просмотр отчетов

## 🚀 Запуск тестов с отчетом

```powershell
.\test_runner.ps1
```

Это создаст отчеты в папке `test_reports/`:
- **Текстовый отчет** с полным выводом
- **JSON отчет** со структурированными данными

## 📊 Быстрый просмотр результатов

```powershell
.\quick_test.ps1
```

Показывает краткую сводку без сохранения файлов.

## 📈 Просмотр отчетов

### Последний отчет
```powershell
.\test_reports\view_report.ps1
```

### Отчет за дату
```powershell
.\test_reports\view_report.ps1 -Date "2025-01-15"
```

### Сводка за период
```powershell
# За последние 7 дней
.\test_reports\generate_summary.ps1

# За последние 30 дней  
.\test_reports\generate_summary.ps1 -Days 30
```

## 📋 Что показывают отчеты

### Краткая сводка
- Общее количество тестов
- Количество успешных тестов
- Количество проваленных тестов
- Процент успеха

### Детальная информация
- Список всех проваленных тестов
- Описание ошибок
- Полный вывод тестов

### JSON формат
```json
{
  "timestamp": "2025-01-15 14:30:00",
  "date": "2025-01-15",
  "summary": {
    "total": 70,
    "passed": 61,
    "failed": 9,
    "success_rate": 87.14
  },
  "failed_tests": [...],
  "failed_test_details": [...]
}
```

## 🔍 Пример использования

1. **Запустить тесты:**
   ```powershell
   .\test_runner.ps1
   ```

2. **Посмотреть результаты:**
   ```powershell
   .\test_reports\view_report.ps1
   ```

3. **Проанализировать тренды:**
   ```powershell
   .\test_reports\generate_summary.ps1 -Days 7
   ```

## ⚠️ Решение проблем

Если скрипты не запускаются:
1. Проверьте права выполнения: `Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser`
2. Убедитесь, что Flutter установлен: `flutter --version`
3. Установите зависимости: `flutter pub get`

## 📝 Примечания

- Отчеты сохраняются в `test_reports/` с временной меткой
- JSON отчеты можно использовать для автоматизации
- Текстовые отчеты содержат полный вывод Flutter




