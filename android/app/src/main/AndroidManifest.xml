<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.orpheus_project"> <!-- УБЕДИСЬ, ЧТО PACKAGE СОВПАДАЕТ С ТВОИМ -->

    <!-- ============================================== -->
    <!--                ПРАВА ДОСТУПА                   -->
    <!-- ============================================== -->

    <!-- Сеть и Интернет -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Аудио и Видео (Звонки) -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- Bluetooth (Гарнитуры) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Биометрия (Вход) -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC"/>

    <!-- Энергосбережение (Чтобы не гас экран и проц не спал) -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Вибрация для уведомлений -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Full-Screen Intent для входящих звонков (показ над экраном блокировки) -->
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    
    <!-- Игнорирование оптимизации батареи (критично для Xiaomi/Vivo) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Автозапуск после перезагрузки -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Отключение DND для важных уведомлений (Android 13+) -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- !!! КРИТИЧНО ДЛЯ ФОНОВОЙ РАБОТЫ (ANDROID 14+) !!! -->
    <!-- Без этих прав приложение упадет при сворачивании звонка -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ============================================== -->
    <!--                ПРИЛОЖЕНИЕ                      -->
    <!-- ============================================== -->
    <application
        android:label="Orpheus"
        android:name="${applicationName}"
        android:icon="@mipmap/launcher_icon">

        <!-- Отключение Impeller (по твоему запросу, для стабильности на старых GPU) -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="false" />

        <!-- ОСНОВНАЯ АКТИВНОСТЬ -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <!-- Запуск с иконки -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- Deep Links (для orpheus://activate/...) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="orpheus" />
            </intent-filter>
        </activity>

        <!-- МЕТА-ДАННЫЕ FLUTTER -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- !!! СЛУЖБА ДЛЯ ФОНОВОЙ РАБОТЫ (FOREGROUND SERVICE) !!! -->
        <!-- Это то, что держит звонок живым. Обязательно foregroundServiceType -->
        <!-- 
            flutter_background_service использует один сервис для всех конфигураций.
            Тип сервиса определяется в коде через AndroidConfiguration.
            Для NotificationForegroundService используется dataSync,
            для BackgroundCallService - microphone|connectedDevice.
        -->
        <service
            android:name="id.flutter.flutter_background_service.BackgroundService"
            android:foregroundServiceType="microphone|connectedDevice|dataSync"
            android:exported="true"
            />

        <!-- Ресивер для автозапуска после перезагрузки -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <!-- Для Xiaomi -->
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

    </application>

    <!-- Разрешаем открывать ссылки и обрабатывать текст -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="http" />
        </intent>
    </queries>
</manifest>